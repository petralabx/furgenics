{%- comment -%}
  Furgenics — value-math snippet
  Renders invariant value claims with consistent phrasing. One source of
  truth for product properties that don't change unless the formulation
  changes (dilution ratio, working gallons per bottle, origin, etc.).

  Why a snippet?
  - Consistent phrasing across every page (pillars, PDPs, blog, FAQs)
  - If a claim changes (e.g., reformulation), one update propagates everywhere
  - Forbidden-phrase guard: claims are pre-approved against brand-voice.md

  Usage:
    Furgenics is a {% render 'value-math', claim: 'dilution-ratio' %}.
    One gallon makes {% render 'value-math', claim: 'working-gallons-per-bottle' %}.

  Parameters:
    claim     (string)   Key from the approved list below. Required.
    inline    (boolean)  Render plain text only, no wrapper span. Default true.

  Available claims (keep this list and the case statement in sync):
    - dilution-ratio
    - dilution-ratio-bare
    - working-gallons-per-bottle
    - per-working-gallon-cost-narrative
    - made-in-canada
    - professional-grade
    - pro-vs-retail-positioning
    - washes-small / washes-medium / washes-large / washes-deshed
    - usage-oz-small / usage-oz-medium / usage-oz-large / usage-oz-deshed

  Wash-count and usage-oz claims render from snippets/wash-economics.liquid.
  Cost-per-wash is NOT a value-math claim (it depends on live price) — use
  [[COST:handle:tier]] via token-substitution.liquid.

  Adding a new claim:
    1. Confirm the claim is substantiated (no exclusivity claims, no medical claims).
    2. Add it to BOTH the comment list above AND the case statement below.
    3. Document the source/evidence in knowledge/content-style-guide.md.
    4. Get Stephen's approval (Class C strategic content).

  Missing claim behavior: renders a visible placeholder so QA catches it.
{%- endcomment -%}

{%- assign _output = '' -%}
{%- case claim -%}
  {%- when 'dilution-ratio' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'dilution-ratio-bare' -%} concentrate{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'dilution-ratio-bare' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'dilution-ratio-bare' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'working-gallons-per-bottle' -%}
    {%- capture _wg -%}{%- render 'wash-economics', claim: 'working-gallons-number' -%}{%- endcapture -%}
    {%- assign _wg = _wg | strip -%}
    {%- assign _output = 'up to ' | append: _wg | append: ' working gallons per bottle at professional dilution' -%}
  {%- when 'washes-small' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'washes', tier: 'small' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'washes-medium' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'washes', tier: 'medium' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'washes-large' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'washes', tier: 'large' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'washes-deshed' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'washes', tier: 'deshed' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'usage-oz-small' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'usage-oz', tier: 'small' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'usage-oz-medium' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'usage-oz', tier: 'medium' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'usage-oz-large' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'usage-oz', tier: 'large' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'usage-oz-deshed' -%}
    {%- capture _output -%}{%- render 'wash-economics', claim: 'usage-oz', tier: 'deshed' -%}{%- endcapture -%}
    {%- assign _output = _output | strip -%}
  {%- when 'per-working-gallon-cost-narrative' -%}
    {%- assign _output = 'roughly the cost of a cup of coffee per working gallon at pro dilution' -%}
  {%- when 'made-in-canada' -%}
    {%- assign _output = 'Made in Canada' -%}
  {%- when 'professional-grade' -%}
    {%- assign _output = 'built for professional salons' -%}
  {%- when 'pro-vs-retail-positioning' -%}
    {%- assign _output = 'a professional concentrate designed for salon volume — not a retail bottle in a bigger size' -%}
{%- endcase -%}

{%- if _output == '' -%}
  <span class="value-math-missing" data-claim="{{ claim }}">[unknown value-math claim: {{ claim }}]</span>
{%- else -%}
  {%- if inline == false -%}
    <span class="value-math" data-claim="{{ claim }}">{{ _output }}</span>
  {%- else -%}
    {{- _output -}}
  {%- endif -%}
{%- endif -%}
